import React from 'react';
import { IndexRoute } from 'react-router';

import FunctionalComplex from '../components/functional-complex';

export default (
  <IndexRoute component={FunctionalComplex} />
);
